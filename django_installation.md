# Setting Up Django 

```
sudo apt update
python3 -m pip install --upgrade pip
python3 --version
pip3 install Django
django-admin --version
echo 'Or run the below command to know the version of Django'
echo 'python -c "import django; print(django.get_version())"'
pip3 install mysqlclient
```
